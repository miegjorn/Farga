# Farga — Fractal Project Context Substrate

**Farga** (Occitan: *forge*) is the fractal project context substrate and collective-memory layer.

Projects have nested components as first-class peers, each with their own context-map, agent, and lifecycle.
